package Model;

/**
 * Interface used as helper for other model classes
 * It defines common behaviour for client, product, and order objects.
 */
public interface ObjectModel {
    //empty parent class for client, product and order
}
